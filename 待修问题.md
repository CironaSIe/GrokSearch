# 待修问题

> 从 GrokSearch-rs 源码分析中提取的可借鉴设计，按优先级排序。

---

## 1. SourceExtractor 特质 + SourceRouter（高优先级）

### GrokSearch-rs 的做法

定义 `trait SourceExtractor`，包含三个方法：
- `fn matches(&self, url: &Url) -> bool` — 同步、廉价的 URL 检查，不做网络请求
- `fn kind(&self) -> SourceType` — 返回枚举标签（GitHubIssue / StackExchange / arXiv / Wikipedia / Generic）
- `async fn fetch_render(&self, client, url, caps) -> Result<String>` — 通过对应平台的原生 API（GitHub Issues API、StackExchange API、arXiv API、Wikipedia API）获取结构化数据，转成 Markdown

`SourceRouter` 持有有序 `Vec<Box<dyn SourceExtractor>>`，构造时按优先级排列：
```
[GithubIssueExtractor, GithubPrExtractor, StackExchangeExtractor,
 ArxivExtractor, WikipediaExtractor]
```

统一入口 `web_fetch`：
1. 第一个 `matches(url)` 返回 true 的 specialist 优先尝试
2. 成功 → 使用 specialist 的结构化 Markdown
3. 匹配但失败/空 → 降级通用链路，暴露原因
4. 无匹配 → 静默降级（`NO_SPECIALIST_MATCH` sentinel）
5. URL 格式错误 → 直接降级（不视为 specialist 错误）

`SourceType` 枚举全局流通，通过 `as_str()` 返回可读标签。`fallback_reason` 直接拼接 `"{source_type} {reason}"`，无需字符串匹配。

### 我们的现状

`web_fetch` 走 Tavily → Firecrawl 链路，对 GitHub Issue、Wikipedia、arXiv 等站点不做特殊处理。Tavily 对这些站点的渲染质量不可控。

### 移植方案

1. 定义 `SourceExtractor` 抽象基类，`match(url: str) -> bool` + `fetch_render(url: str) -> str | None`
2. 注册表模式：`SourceRouter` 持有有序 extractor 列表，`web_fetch` 入口加入 `resolve_specialist(url)` 钩子
3. 不改变现有 Tavily/Firecrawl 链路，specialist 返回 None 时自动降级

---

## 2. GrokSearchError 枚举 + JSON-RPC 错误码映射（中优先级）

### GrokSearch-rs 的做法

43 行 `thiserror` derive 枚举，每个变体绑定 JSON-RPC 2.0 标准错误码：

| 变体 | 码值 | 含义 |
|------|------|------|
| `Parse` | -32700 | JSON 解析错误 |
| `InvalidParams` | -32602 | 参数校验失败 |
| `NotFound` | -32004 | 资源未找到 |
| `Timeout` | -32002 | 上游超时 |
| `Provider` | -32001 | 上游服务错误 |
| `OAuth` | -32005 | OAuth 失败 |
| `MissingConfig` | -32003 | 配置缺失 |

Client 侧收到 MCP error response 即可通过 code 区分故障类型，无需 parse message。`Result<T>` 类型别名全局使用。

### 我们的现状

错误散布在各处，无统一码表。不同工具返回的错误格式不一致。

### 移植方案

创建 `grok_search/errors.py`，包含：
- `GrokSearchError(Exception)` 基类 + 各子类，每类绑定 `JSONRPC_CODE` 类属性
- `error_to_jsonrpc(e)` 工具函数，供 `server.py` 在处理异常时调用
- 当前已有的异常类型映射到此枚举

---

## 3. doctor 工具的结构化状态报告（中优先级）

### GrokSearch-rs 的做法

`doctor()` 返回一个 JSON，如实报告：
- `provider` / `transport` — 当前实际使用的 AI 后端（Responses API vs Chat Completions）
- `grok.api_url` — 脱敏后的 API URL（根据 transport 动态取）
- `grok.model` — 当前模型
- `grok.auth_mode` — `api_key` | `oauth`（只报状态，不做判断）
- `grok.auth_file` — 认证文件路径
- `grok.web_search_enabled` / `x_search_enabled`
- `grok.reachable` + `detail` — 连通性探测结果
- `tavily` / `firecrawl` — 各自的 API URL、可达性、详细探测信息，未配置时报 `skipped`

其中 `probe_grok` 对 `/models` 端点发真实请求验证连通性。

### 我们的现状

`get_config_info` 返回配置信息但缺乏可达性实时探测；不区分 transport 路径（Responses vs Chat）；认证方式固定假设 API key。

### 移植方案

1. `get_config_info` 增加实时连通性探测（Grok + Tavily + Firecrawl 各自独立 probe）
2. 增加 `auth_mode` 字段（当前恒为 `api_key`，但为将来预留）
3. 增加 `transport` 字段（`responses` | `chat_completions`）
4. 增加 `decon_pipeline_enabled` 布尔字段

---

## 4. SourceType 枚举 + as_str() 方法（低优先级）

### GrokSearch-rs 的做法

```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SourceType {
    GithubIssue, GithubPull, Stackexchange, Arxiv, Wikipedia, Generic,
}

impl SourceType {
    pub fn as_str(&self) -> &'static str {
        match self {
            SourceType::GithubIssue => "github_issue",
            // ...
        }
    }
}
```

枚举在结果结构体中携带，下游按标签分流。`as_str` 返回值与 serde serialization 一致。

### 我们的现状

source type 用字符串字面量传递，无类型安全，无单一真源。

### 移植方案

Python 3.11+ 用 `StrEnum`：

```python
class SourceType(StrEnum):
    GITHUB_ISSUE = "github_issue"
    GITHUB_PULL = "github_pull"
    STACKEXCHANGE = "stackexchange"
    ARXIV = "arxiv"
    WIKIPEDIA = "wikipedia"
    GENERIC = "generic"
```

枚举在 `Source` 结果模型中携带。`resolve_specialist` 返回值携带 `(content, SourceType)` 二元组。

---

## 附：reasoning_effort 改造方案（实测验证）

### 实测结果

对 `https://p01--grok--46v5dsmhrdgh.code.run/v1/responses` 三次实测：

| 参数 | output_tokens | 长度 | 自动搜索 |
|------|--------------|------|---------|
| `max_tokens=200, reasoning=high` | 1708 | 2575字 | 6次 |
| `max_tokens=1000, reasoning=low` | 1684 | 2519字 | 3次 |
| 无参数 | 2122 | 3664字 | 3次 |

关键发现：
1. **`reasoning: {"effort": "high"}` 被 API 接受**，实际效果有限——无论 low/high 模型都自主触发了 web_search。
2. **`max_output_tokens` 被 API 接受但不贯彻**——200 和 1000 返回几乎相同的 token 数（~1700），说明服务端内部有独立的上限控制或忽略了这个参数。
3. 响应中包含 `reasoning` block（搜索查询记录），对透明度和去污管道有价值。

### 改造方案

**reasoning_effort**：将当前 prompt hack 替换为 API 参数，不双发。

- Responses API 路径：`payload["reasoning"] = {"effort": effort}`，去掉 `_build_reasoning_prompt` 的注入
- Chat Completions 路径：保留 `_build_reasoning_prompt` 作为 fallback（无原生 `reasoning` 参数）
- 理由：prompt hack 和 API 参数同时发送自相矛盾（prompt 说 "Be concise" 但参数说 `effort=high`）

**max_output_tokens**：不做处理。原因：
- 上游接受参数但不贯彻，prompt hack 控制长度也不可靠
- MCP 结果给 LLM 消费而非人类阅读，长度差异不影响功能
- GrokSearch-rs 做客户端侧截断是因为它走搜索 API 返回结构化片段，场景不同
