# 待修问题

> GrokSearch 的 `web_fetch` 走 Tavily → Firecrawl → Grok 通用爬取链路，对 GitHub Issues、arXiv、Wikipedia 等特定平台，通用爬取渲染质量差、结构混乱。需要为高频信源添加 specialist content extractor，走原生 API 获取结构化内容，降级到通用链路兜底。

---

## 信源筛选（从 LLM 使用频率出发）

### 做：

| 信源 | 频率场景 | API 状态 | 价值 |
|------|---------|----------|------|
| **GitHub Issues/PRs** | 编码场景高频——读 bug 报告、PR diff、讨论串 | 需 token（免费，60req/h 无 token，5k 有 token） | ★★★ |
| **arXiv** | 学术搜索高频命中——fetch 摘要/元数据 | **无认证**，1req/3s | ★★★ |
| **Wikipedia** | 研究起步参考——初步了解但不当学术信源 | **无认证**，200req/min（设 UA） | ★★☆ |
| **Hacker News** | 技术讨论引用密集区 | **无认证**，Firebase API | ★★☆ |

### 跳过：

| 信源 | 原因 |
|------|------|
| StackExchange | LLM 训练数据已覆盖常见 Q&A，fetch 频率低 |
| PyPI/npm | 搜索库时会被 fetch 但 Tavily 渲染质量已够 |
| Reddit/Twitter/X | 被 xAI 搜索和 x_search 工具覆盖，无需重复 |
| 知乎/CSDN/Medium | 无可用 API，只能通用爬取 |

---

## 设计原则

1. **透明** — Specialist 对 LLM 不可见。LLM 调 `web_fetch(url)`，router 自动匹配 specialist 或降级
2. **容错** — 任何异常（超时/限频/网络错误/API 变更）静默降级到 Tavily/Firecrawl 链路
3. **代理兼容** — 通过环境变量 `SPECIALIST_HTTPS_PROXY` / `SPECIALIST_HTTP_PROXY` 控制
4. **无认证先行** — arXiv / Wikipedia / HN 不需要用户配置任何 token
5. **GitHub token 可选** — 有 token 提升到 5000req/h，无 token 仍可用（60req/h）
